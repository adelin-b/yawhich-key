name: kitty


window_class:
  - kitty

dependencies:
  # The command to test if it exist or not
  - command: kitty

keymaps:
    +help: xdotool key ctrl+shift+h h
    
    +/-search: xdotool key ctrl+shift+h slash
    
    +insert: xdotool key ctrl+shift+u
    
    +url: xdotool key ctrl+shift+e
    
